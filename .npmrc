# Expo and React Native both resolve modules by walking up directories, which
# symlinked stores break. A flat node_modules keeps Metro able to find things.
node-linker=hoisted

# Store kept inside the repo so installs behave the same for everyone.
store-dir=.pnpm-store
